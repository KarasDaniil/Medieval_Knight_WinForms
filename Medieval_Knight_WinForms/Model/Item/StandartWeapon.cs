namespace Medieval_Knight_WinForms.Model.Item
{
    class StandartWeapon : AbstractWeapon
    {
        public StandartWeapon(string name, decimal cost, int atack, int damage) : base(name, cost, atack, damage)
        {
        }
    }
}
